# Two non public classes without a public class will compile

This example is used to verify that public classes are not mandatory. 
In fact, two non public classes will compile just fine.

javac -d out \*.java 