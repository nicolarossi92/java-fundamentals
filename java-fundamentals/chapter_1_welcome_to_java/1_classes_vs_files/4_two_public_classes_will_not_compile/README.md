# Two public class will not compile in the same file

This example is used to verify that there can't be more than one public class inside the same file. 
Therefore, two public classes in a file will not compile.

javac -d out \*.java 