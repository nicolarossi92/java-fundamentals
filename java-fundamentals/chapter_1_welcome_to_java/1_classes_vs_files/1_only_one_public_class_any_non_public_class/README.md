# A file with a single public class and multiple non public classes will compile 

This example is used to verify that a single public class
and any number of non public classes in the same file will compile.

javac -d out \*.java 