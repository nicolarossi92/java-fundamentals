# Mandatory import directive

If we want to access a class outside the java.lang package, we must provide
the corresponding import directive. It will raise a compilation error otherwise.

javac -d out \*.java