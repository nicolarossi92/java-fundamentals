# Passing arguments to main method

This test ensures that arguments passed to the program are used effectively by the program itself

javac -d out \*.java
java -cp out PassArgumentsToMainMethod "Hello!"