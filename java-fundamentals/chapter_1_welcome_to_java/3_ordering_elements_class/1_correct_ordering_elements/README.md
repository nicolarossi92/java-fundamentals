# Correct ordering of elements in a java class file

The correct order of elements inside a class, where a package is optional,
imports are optional if no classes are imported and a class definition is mandatory

javac -d out \*.java;