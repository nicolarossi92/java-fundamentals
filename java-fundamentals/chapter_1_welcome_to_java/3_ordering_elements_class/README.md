# Ordering of elements in a class section

This section contains tests to validate the rules for the
correct order of elements inside a class, for it to compile
1. Example of a correct ordering of elements
2. Incorrect order won't compile