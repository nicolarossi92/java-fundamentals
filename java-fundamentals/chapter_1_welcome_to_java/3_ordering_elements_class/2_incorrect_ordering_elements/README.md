# Example of incorrect ordering of elements in a java class file

The PIC (Package/Import/Class) ordering is mandatory and this means
a compilation error for any violation of this ordering

javac -d out \*.java;